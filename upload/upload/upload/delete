<?php

$uploadDir = "uploads/";

if (isset($_POST['file'])) {

    $file = basename($_POST['file']); // verhindert Pfad-Manipulation
    $path = $uploadDir . $file;

    if (file_exists($path)) {
        unlink($path);
    }
}

header("Location: index.php");
exit;
?>
